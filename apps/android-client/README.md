# Ciphered Souls Android Client

This directory is reserved for the maintainable Android/RuneLite-derived mobile client source.

The APK binary itself should not be committed here. Once the Drive APK is visible, inspect it to identify package name, revision, endpoint configuration, bundled native libraries, and whether corresponding source exists.
