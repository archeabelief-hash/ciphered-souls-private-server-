# Ciphered Souls Game Server

This directory is reserved for the authoritative RSPS game server source once the correct server tree is identified and separated from legacy launcher/browser experiments.

Do not place the large cache in normal Git history. Keep source, scripts, configuration, protocol definitions, and tests here; fetch or mount large cache/runtime assets during development setup.
